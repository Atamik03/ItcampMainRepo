node_modules/
dist/
.git
# NOTE: patterns are relative to the build context root (repo root when used
# via `docker compose build`, per docker-compose.yml `context: .`), not to
# this file's own directory -- matches the root .dockerignore.
.env
.env.*
secrets/
